# V8 research provenance

The canonical V8 research/deployment operations documentation is `docs/V8_RESEARCH_OPERATIONS.md`.

This compatibility pointer is retained because `ROADMAP.md` linked this path during the V8 transition. Keep detailed V8 operational guidance in the canonical document to avoid two sources drifting apart.

See also `docs/DEPLOYMENT.md` for the provenance-aware Cloudflare deployment guard and operator overrides.
